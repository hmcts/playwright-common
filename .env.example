# Example environment configuration for @hmcts/playwright-common consumers

# Logging
LOG_LEVEL=info
LOG_FORMAT=json
LOG_REDACTION=on
PLAYWRIGHT_DEBUG_API=0

# IDAM endpoints (use HTTPS)
IDAM_WEB_URL=https://idam-web-public.aat.platform.hmcts.net
IDAM_TESTING_SUPPORT_URL=https://idam-testing-support-api.aat.platform.hmcts.net

# S2S gateway (secure; use proper internal DNS)
S2S_URL=https://rpe-service-auth-provider-aat.platform.hmcts.net/testing-support/lease
# Provide S2S secret via env, constructor, or per call
# S2S_SECRET=replace-with-key-via-keyvault

# Optional retry for S2S token fetch
S2S_RETRY_ATTEMPTS=3
S2S_RETRY_BASE_MS=200
